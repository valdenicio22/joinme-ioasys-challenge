import { Story, Meta } from '@storybook/react'
import LocationIcon from '.'

export default {
  title: 'LocationIcon',
  component: LocationIcon
} as Meta

export const Default: Story = (args) => <LocationIcon {...args} />
