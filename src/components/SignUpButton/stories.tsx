import { Story, Meta } from '@storybook/react'
import SignUpButton from '.'

export default {
  title: 'SignUpButton',
  component: SignUpButton
} as Meta

export const Default: Story = () => <SignUpButton />
