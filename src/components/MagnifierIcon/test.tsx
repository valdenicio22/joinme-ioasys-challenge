import { render } from '@testing-library/react'

import MagnifierIcon from '.'

describe('<MagnifierIcon />', () => {
  it('should render the magnifierIcon', () => {
    render(<MagnifierIcon />)
  })
})
